# CORS -- lock this down to your actual frontend origin in production
FRONTEND_ORIGIN=https://yourapp.com

# Local testing only: folder where finished clips/converts are also saved on disk.
# Browser still receives the file; this is a second copy for easy inspection.
# Windows example (use your real path):
LOCAL_OUTPUT_DIR=C:\Users\Victorjames\Desktop\whop clips

# Optional: S3-compatible object storage for finished clips in production
# S3_BUCKET=
# S3_ACCESS_KEY=
# S3_SECRET_KEY=
